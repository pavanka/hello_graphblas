# Exploration of GraphBlas

### Docker
```
just docker-build
just docker-shell
```

### Local
To set up the environment run this command the first time
`just setup`

To execute a target run
`just run {target}`

Example: to run activities execute `just run activities.py`
